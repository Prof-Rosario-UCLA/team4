# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: oversea-service
spec:
  selector:
    app: oversea-app
  ports:
  - name: web
    port: 80
    targetPort: 3000
  - name: ai
    port: 8000
    targetPort: 8000
  type: LoadBalancer